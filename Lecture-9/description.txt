Functions 
Why do we need functions? 
- for reusability 
What are functions?
- piece of program which perform certain task 
- input (parameters / arguments)
- process them 
- display the data / return the data 
- a function can return more than one value 
syntax: it is the grammar of a programming language 
def functionName(p1,p2,p3,...):
    # logic 
    return value (optional) 
if you skip the return statement it will return 
None by default 

Heap memory: where all you're variable will be stored 
- when ever you assign a value to a variable 
- first this it check is if the value is already their 
in the heap memory 
- if you have the value you will not create a new space 
instead you will assign that memory to the varaible 
ex: 
12 -> (64)
n1 = 64 -> location 12 in the memory 
n2 = 64 -> not create new memory instead it will assign 12 to it 
shared access variable share access to same memory location 
e1 = 1000 (id = 30)
e2 = 1000  (id = 30)
when you increase +500 for e2 you think it will effect e1 and e2 
heap memory is immuatable (you cannot change data once it is stored)
if you make any changes it will create new space and assign new id to it 
e2 = e2 + 500 -> create new memory for 1500 (60)
